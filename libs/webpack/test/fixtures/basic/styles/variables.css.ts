import { defineVariables } from '@salty-css/core/factories';

export const variables = defineVariables({
  color: {
    primary: '#f00',
    surface: '#fff',
  },
  spacing: {
    small: '4px',
    medium: '8px',
  },
});
